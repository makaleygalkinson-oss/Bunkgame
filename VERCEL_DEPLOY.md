# 🚀 Деплой на Vercel

Полная инструкция по развёртыванию проекта на Vercel.

## 📋 Предварительные требования

1. ✅ Аккаунт на [Vercel](https://vercel.com)
2. ✅ Проект на [Supabase](https://supabase.com)
3. ✅ GitHub репозиторий (рекомендуется) или готовность загрузить проект напрямую

## 🔧 Шаг 1: Подготовка проекта

### 1.1. Убедитесь, что проект готов к деплою

```bash
# Проверьте, что проект собирается без ошибок
npm run build
```

### 1.2. Настройте Supabase для продакшена

В Supabase Dashboard перейдите в **Authentication > URL Configuration** и добавьте:

**Site URL:**
```
https://your-project.vercel.app
```

**Redirect URLs:**
```
https://your-project.vercel.app/auth/callback
https://your-project.vercel.app/**
```

> ⚠️ **Важно:** Замените `your-project.vercel.app` на ваш реальный домен Vercel после первого деплоя.

## 🚀 Шаг 2: Деплой через Vercel Dashboard

### Вариант A: Через GitHub (рекомендуется)

1. **Подключите GitHub репозиторий:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите **"Add New..."** → **"Project"**
   - Выберите ваш GitHub репозиторий
   - Импортируйте проект

2. **Настройте проект:**
   - **Framework Preset:** Next.js (определится автоматически)
   - **Root Directory:** `./` (если проект в корне)
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)

3. **Добавьте переменные окружения:**
   - В разделе **"Environment Variables"** добавьте:
     ```
     NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
     NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
     ```
   - Выберите **"Production"**, **"Preview"** и **"Development"** для каждой переменной

4. **Деплой:**
   - Нажмите **"Deploy"**
   - Дождитесь завершения сборки

### Вариант B: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   vercel
   ```
   
   При первом деплое:
   - Выберите **"Set up and deploy"**
   - Подтвердите настройки проекта
   - Добавьте переменные окружения при запросе

4. **Деплой в продакшн:**
   ```bash
   vercel --prod
   ```

## 🔐 Шаг 3: Настройка переменных окружения

### В Vercel Dashboard:

1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте следующие переменные:

| Переменная | Значение | Где найти |
|-----------|----------|-----------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://xxxxx.supabase.co` | Supabase Dashboard → Settings → API → Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJhbGci...` | Supabase Dashboard → Settings → API → anon public key |

3. Выберите окружения:
   - ✅ **Production**
   - ✅ **Preview** 
   - ✅ **Development**

4. Нажмите **"Save"**

### Через Vercel CLI:

```bash
vercel env add NEXT_PUBLIC_SUPABASE_URL
vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY
```

## 🔄 Шаг 4: Обновление Supabase Redirect URLs

После первого деплоя получите ваш домен Vercel (например: `https://cyberpunk-auth.vercel.app`)

1. В Supabase Dashboard: **Authentication** → **URL Configuration**
2. Обновите **Redirect URLs:**
   ```
   https://your-project.vercel.app/auth/callback
   https://your-project.vercel.app/**
   ```
3. Обновите **Site URL:**
   ```
   https://your-project.vercel.app
   ```

### Для Google OAuth:

Если используете Google OAuth, обновите Redirect URL в Google Cloud Console:

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. **APIs & Services** → **Credentials**
3. Найдите ваш OAuth 2.0 Client ID
4. Добавьте в **Authorized redirect URIs:**
   ```
   https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
   ```
   (Это URL Supabase, не Vercel!)

## ✅ Шаг 5: Проверка деплоя

1. Откройте ваш сайт: `https://your-project.vercel.app`
2. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Регистрация работает
   - ✅ Вход работает
   - ✅ OAuth работает (если настроен)
   - ✅ Редирект на `/lobby` после входа работает

## 🔄 Автоматический деплой

После подключения GitHub репозитория, Vercel автоматически деплоит:
- **Production:** при пуше в `main`/`master` ветку
- **Preview:** при создании Pull Request или пуше в другие ветки

## 📝 Полезные команды Vercel CLI

```bash
# Просмотр деплоев
vercel ls

# Просмотр логов
vercel logs

# Открыть проект в браузере
vercel open

# Удалить деплой
vercel remove

# Просмотр переменных окружения
vercel env ls
```

## 🐛 Решение проблем

### Ошибка: "Environment variables not found"
- Убедитесь, что переменные добавлены в Vercel Dashboard
- Проверьте, что они доступны для нужного окружения (Production/Preview/Development)
- Пересоберите проект после добавления переменных

### OAuth не работает
- Проверьте Redirect URLs в Supabase
- Убедитесь, что Site URL правильный
- Проверьте логи в Vercel Dashboard → Deployments → Logs

### Сборка падает
- Проверьте логи сборки в Vercel Dashboard
- Убедитесь, что все зависимости в `package.json`
- Проверьте, что Node.js версия совместима (18+)

## 🎉 Готово!

Ваш проект теперь развёрнут на Vercel! 

**Следующие шаги:**
- Настройте кастомный домен (опционально)
- Настройте мониторинг и аналитику
- Настройте CI/CD для автоматических деплоев

