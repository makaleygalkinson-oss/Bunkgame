# 💻 Локальная разработка - Подробная инструкция

## 📍 Где выполнять команды?

Все команды нужно выполнять в **терминале** (командной строке) в папке вашего проекта.

### Windows:
1. Откройте **PowerShell** или **Command Prompt** (cmd)
2. Перейдите в папку проекта:
   ```powershell
   cd "C:\Users\Пётр Дулов\Desktop\Новая папка (9)"
   ```

### Или используйте встроенный терминал в VS Code:
1. Откройте проект в VS Code
2. Нажмите `Ctrl + ~` (тильда) или меню **Terminal → New Terminal**
3. Терминал откроется в папке проекта автоматически

---

## ✅ Шаг 1: Проверка Node.js

Сначала проверьте, установлен ли Node.js:

```bash
node --version
npm --version
```

Если команды не работают, установите Node.js:
- Скачайте с [nodejs.org](https://nodejs.org/) (рекомендуется LTS версия)
- Установите, следуя инструкциям
- Перезапустите терминал

---

## 📦 Шаг 2: Установка зависимостей

В терминале выполните:

```bash
npm install
```

Эта команда:
- Прочитает `package.json`
- Установит все необходимые библиотеки в папку `node_modules`
- Займет несколько минут при первом запуске

**Где выполнять:** В терминале, в папке проекта

---

## 🔐 Шаг 3: Создание файла .env.local

Вам нужно создать файл с переменными окружения для подключения к Supabase.

### Вариант 1: Через VS Code (рекомендуется)

1. В VS Code нажмите правой кнопкой на корне проекта
2. Выберите **New File**
3. Назовите файл: `.env.local` (с точкой в начале!)
4. Откройте файл и вставьте:

```
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-длинный-ключ
```

5. Замените значения на реальные из вашего Supabase проекта
6. Сохраните файл (Ctrl + S)

### Вариант 2: Через терминал (Windows PowerShell)

```powershell
# Создать файл
New-Item -Path ".env.local" -ItemType File

# Открыть в блокноте для редактирования
notepad .env.local
```

Затем вставьте переменные окружения и сохраните.

### Вариант 3: Через проводник Windows

1. Откройте папку проекта в проводнике
2. Создайте новый текстовый файл
3. Назовите его `.env.local` (важно: с точкой в начале!)
4. Если Windows не дает создать файл с точкой, создайте `env.local`, затем переименуйте через командную строку:
   ```bash
   ren env.local .env.local
   ```
5. Откройте файл в блокноте и вставьте переменные

### 📝 Содержимое файла .env.local:

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx
```

**Где взять эти значения:**
1. Зайдите на [supabase.com](https://supabase.com)
2. Откройте ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

## 🚀 Шаг 4: Запуск dev сервера

В терминале выполните:

```bash
npm run dev
```

Вы увидите что-то вроде:
```
  ▲ Next.js 14.0.4
  - Local:        http://localhost:3000
  - Ready in 2.3s
```

**Где выполнять:** В терминале, в папке проекта

---

## 🌐 Шаг 5: Открытие в браузере

1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите по адресу: **http://localhost:3000**
3. Вы должны увидеть главную страницу игры "Бункер"

---

## 🔄 Что делать дальше?

### Во время разработки:

- **Терминал должен быть открыт** - сервер работает постоянно
- **Изменения в коде** автоматически обновляются в браузере (Hot Reload)
- **Для остановки** нажмите `Ctrl + C` в терминале

### Структура работы:

```
Терминал (npm run dev) → Запускает сервер
         ↓
Браузер (localhost:3000) → Показывает сайт
         ↓
VS Code (редактирование файлов) → Изменения применяются автоматически
```

---

## ❌ Решение проблем

### Ошибка: "npm не является внутренней или внешней командой"
- Установите Node.js с [nodejs.org](https://nodejs.org/)

### Ошибка: "Cannot find module"
- Выполните `npm install` еще раз
- Убедитесь, что находитесь в правильной папке

### Ошибка подключения к Supabase
- Проверьте правильность URL и ключа в `.env.local`
- Убедитесь, что файл называется именно `.env.local` (с точкой!)
- Перезапустите dev сервер после изменения `.env.local`

### Порт 3000 занят
- Закройте другое приложение на порту 3000
- Или используйте другой порт: `npm run dev -- -p 3001`

### Файл .env.local не виден в проводнике
- Это нормально! Файлы с точкой в начале скрыты по умолчанию
- В VS Code они видны
- В проводнике включите "Показывать скрытые файлы"

---

## 📁 Где находятся файлы?

```
Новая папка (9)/
├── app/              ← Страницы сайта (редактируйте здесь)
├── components/       ← Компоненты React
├── lib/              ← Логика и утилиты
├── .env.local        ← Ваши ключи Supabase (НЕ коммитьте в git!)
├── package.json      ← Зависимости проекта
└── node_modules/    ← Установленные библиотеки (не трогайте)
```

---

## 💡 Полезные команды

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена (проверка)
npm run build

# Остановка сервера
Ctrl + C

# Очистка кэша и переустановка
rm -rf node_modules
npm install
```

---

## ✅ Чек-лист готовности

- [ ] Node.js установлен (`node --version` работает)
- [ ] Зависимости установлены (`npm install` выполнен)
- [ ] Файл `.env.local` создан в корне проекта
- [ ] В `.env.local` указаны правильные Supabase ключи
- [ ] Dev сервер запущен (`npm run dev`)
- [ ] Сайт открывается в браузере на localhost:3000

Готово! Теперь вы можете разрабатывать локально! 🎉

