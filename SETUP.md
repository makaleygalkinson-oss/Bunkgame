# Инструкция по настройке и запуску

## 📋 Предварительные требования

- Node.js 18+ 
- npm или yarn
- Аккаунт Supabase (бесплатный)

## 🚀 Быстрая установка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Перейдите на [supabase.com](https://supabase.com) и создайте новый проект
2. Дождитесь завершения создания проекта (обычно 1-2 минуты)
3. В разделе **"SQL Editor"** → **"New query"** выполните скрипт из файла `supabase/schema.sql`
   - Скопируйте весь содержимое файла
   - Вставьте в редактор и нажмите **"Run"**
4. В разделе **"Settings"** → **"API"** скопируйте следующие значения:
   - **Project URL** → это будет `NEXT_PUBLIC_SUPABASE_URL`
   - **`anon` `public` key** → это будет `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **`service_role` `secret` key** → это будет `SUPABASE_SERVICE_ROLE_KEY` (⚠️ держите в секрете!)

### 3. Настройка для локальной разработки

Создайте файл `.env.local` в корне проекта (для локальной разработки):

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

⚠️ **Важно:** Файл `.env.local` уже добавлен в `.gitignore` и не будет загружен в репозиторий.

### 4. Настройка переменных окружения в Vercel

Поскольку вы деплоите на Vercel, нужно настроить переменные окружения там:

1. Перейдите в ваш проект на [vercel.com](https://vercel.com)
2. Откройте **"Settings"** → **"Environment Variables"**
3. Добавьте следующие переменные:

   | Имя переменной | Значение | Окружение |
   |--------------|----------|-----------|
   | `NEXT_PUBLIC_SUPABASE_URL` | `https://your-project.supabase.co` | Production, Preview, Development |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Ваш anon key из Supabase | Production, Preview, Development |
   | `SUPABASE_SERVICE_ROLE_KEY` | Ваш service_role key из Supabase | Production, Preview, Development |
   | `NEXT_PUBLIC_APP_URL` | `https://your-project.vercel.app` | Production, Preview, Development |

4. После добавления переменных **передеплойте проект**:
   - Перейдите в **"Deployments"**
   - Нажмите на три точки у последнего деплоя
   - Выберите **"Redeploy"**

   Или просто сделайте новый коммит и пуш в репозиторий - Vercel автоматически передеплоит.

### 5. Запуск в режиме разработки (локально)

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 6. Проверка работы после деплоя

После деплоя на Vercel:

1. Откройте ваш сайт по адресу `https://your-project.vercel.app`
2. Попробуйте создать лобби
3. Если видите ошибки в консоли браузера - проверьте:
   - Правильность переменных окружения в Vercel
   - Что SQL скрипт выполнен в Supabase
   - Логи деплоя в Vercel Dashboard

## 🎮 Как играть

1. **Создание лобби:**
   - Нажмите "Создать Лобби"
   - Введите свой ник и название комнаты
   - Выберите активные роли
   - Скопируйте ID комнаты и поделитесь с друзьями

2. **Присоединение к лобби:**
   - Нажмите "Присоединиться к лобби"
   - Введите свой ник и ID комнаты

3. **Начало игры:**
   - Хост нажимает "Начать игру" (минимум 4 игрока)
   - Игрокам раздаются карты
   - Начинается первый раунд

4. **Игровой процесс:**
   - **Фаза 1:** Активный игрок открывает карту бункера
   - **Фаза 2:** Игроки по очереди представляют свои карты (30 сек)
   - **Фаза 3:** Тайное голосование за исключение игрока
   - Повторяется 5 раундов

5. **Победа:**
   - Выживают игроки, оставшиеся после 5 раундов
   - Учитываются условия карт бункера

## 🛠 Разработка

### Структура проекта

```
app/
├── api/game/          # API endpoints для игры
├── game/[gameId]/     # Страница игры
└── page.tsx           # Главная страница

components/
├── GameBoard.tsx      # Основной игровой экран
├── CardDisplay.tsx    # Отображение карт
├── VotingPanel.tsx   # Панель голосования
└── ...

lib/
├── db.ts              # Работа с Supabase
├── types.ts           # TypeScript типы
└── utils.ts           # Утилиты
```

### Добавление новых карт

Отредактируйте `lib/utils.ts` и добавьте карты в объект `CARD_LIBRARY`:

```typescript
export const CARD_LIBRARY: Record<CardType, string[]> = {
  profession: [
    'Врач',
    'Новая профессия', // Добавьте здесь
    // ...
  ],
  // ...
}
```

### API Endpoints

- `POST /api/game/create` - Создание игры
- `GET /api/game/[gameId]` - Получение игры
- `POST /api/game/[gameId]/join` - Присоединение
- `POST /api/game/[gameId]/start` - Запуск игры
- `POST /api/game/[gameId]/vote` - Голосование

## 🚀 Деплой на Vercel (уже настроено)

Если вы уже подключили проект к Vercel, следуйте этим шагам:

### Первоначальная настройка (если ещё не сделано)

1. **Подключение репозитория:**
   - Загрузите код в GitHub/GitLab/Bitbucket
   - В [Vercel Dashboard](https://vercel.com/dashboard) нажмите **"Add New Project"**
   - Импортируйте ваш репозиторий
   - Vercel автоматически определит Next.js проект

2. **Настройка переменных окружения:**
   - После импорта проекта откройте **"Settings"** → **"Environment Variables"**
   - Добавьте все переменные из раздела "Настройка переменных окружения в Vercel" выше
   - ⚠️ Обязательно выберите все окружения: **Production**, **Preview**, **Development**

3. **Первый деплой:**
   - Vercel автоматически запустит деплой после импорта
   - Дождитесь завершения сборки
   - Проверьте работу сайта

### Обновление проекта

После каждого пуша в репозиторий Vercel автоматически:
- Обнаружит изменения
- Запустит новую сборку
- Задеплоит обновлённую версию

Вы можете отслеживать деплои в разделе **"Deployments"**.

### Получение URL продакшена

После первого деплоя:
- Vercel присвоит вашему проекту URL вида: `https://your-project.vercel.app`
- Вы можете настроить кастомный домен в **"Settings"** → **"Domains"**
- Используйте этот URL для переменной `NEXT_PUBLIC_APP_URL` в Vercel

### Полезные команды Vercel CLI (опционально)

Если хотите деплоить через CLI:

```bash
# Установка Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой
vercel

# Деплой в продакшен
vercel --prod
```

## 🐛 Решение проблем

### Ошибка подключения к Supabase

**Локально:**
- Проверьте правильность URL и ключей в `.env.local`
- Убедитесь, что файл `.env.local` находится в корне проекта
- Перезапустите dev-сервер после изменения `.env.local`

**На Vercel:**
- Проверьте переменные окружения в **Settings** → **Environment Variables**
- Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)
- Проверьте, что значения скопированы полностью (без пробелов в начале/конце)
- После изменения переменных **обязательно передеплойте проект**

**Общие:**
- Убедитесь, что проект Supabase активен (не приостановлен)
- Проверьте, что SQL скрипт выполнен в Supabase SQL Editor
- Проверьте логи в Supabase Dashboard → **Logs** → **Postgres Logs**

### Ошибки при деплое на Vercel

- **Build failed:** Проверьте логи сборки в Vercel Dashboard
- **Environment variables missing:** Убедитесь, что все переменные добавлены
- **Database connection error:** Проверьте правильность `NEXT_PUBLIC_SUPABASE_URL` и ключей
- **TypeScript errors:** Исправьте ошибки типов перед деплоем

### Игра не обновляется

- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что API endpoints работают (проверьте Network tab)
- Проверьте логи в Supabase Dashboard → **Logs**
- Проверьте логи деплоя в Vercel Dashboard → **Deployments** → выберите деплой → **Functions**

### Игроки не видят обновления

- В MVP используется polling (обновление каждые 2 секунды)
- Для реалтайм нужен WebSocket (Phase 2)
- Если обновления не приходят, проверьте:
  - Работает ли API endpoint `/api/game/[gameId]`
  - Нет ли ошибок в консоли браузера
  - Правильно ли настроены CORS в Supabase (если нужно)

### Переменные окружения не работают

- Переменные с префиксом `NEXT_PUBLIC_` доступны в браузере
- Переменные без префикса доступны только на сервере
- После изменения переменных в Vercel нужно передеплоить проект
- В локальной разработке перезапустите `npm run dev` после изменения `.env.local`

## 📝 TODO

- [ ] WebSocket для реалтайм синхронизации
- [ ] История игр
- [ ] Статистика игроков
- [ ] Чат в игровой комнате
- [ ] Кастомизация карт через админ-панель
- [ ] Мобильная версия

## 📄 Лицензия

MIT

