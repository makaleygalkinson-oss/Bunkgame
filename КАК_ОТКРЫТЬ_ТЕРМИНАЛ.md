# 🖥️ Как открыть терминал для разработки

## ❌ Ошибка: "Uncaught SyntaxError" или "ReferenceError"

Если вы видите такую ошибку, значит вы выполнили команду **не в том месте**!

**НЕ выполняйте команды:**
- ❌ В консоли браузера (F12 → Console)
- ❌ В адресной строке браузера
- ❌ В текстовом редакторе

**Выполняйте команды ТОЛЬКО в терминале операционной системы!**

---

## ✅ Правильный способ: Открыть терминал

### Способ 1: VS Code (САМЫЙ ПРОСТОЙ) ⭐

1. **Откройте VS Code**
2. **Откройте папку проекта:**
   - File → Open Folder
   - Выберите папку: `Новая папка (9)`

3. **Откройте терминал:**
   - Нажмите `Ctrl + ~` (тильда, клавиша над Tab)
   - ИЛИ меню: **Terminal → New Terminal**
   - ИЛИ вкладка: **Terminal** внизу экрана

4. **Вы увидите что-то вроде:**
   ```
   PS C:\Users\Пётр Дулов\Desktop\Новая папка (9)>
   ```

5. **Теперь можно выполнять команды!**
   ```bash
   node --version
   npm --version
   npm install
   ```

---

### Способ 2: PowerShell (Windows)

1. **Нажмите `Win + X`** (или правой кнопкой на меню Пуск)
2. **Выберите "Windows PowerShell"** или "Terminal"
3. **Перейдите в папку проекта:**
   ```powershell
   cd "C:\Users\Пётр Дулов\Desktop\Новая папка (9)"
   ```
4. **Теперь можно выполнять команды!**

---

### Способ 3: Command Prompt (cmd)

1. **Нажмите `Win + R`**
2. **Введите:** `cmd` и нажмите Enter
3. **Перейдите в папку проекта:**
   ```cmd
   cd "C:\Users\Пётр Дулов\Desktop\Новая папка (9)"
   ```
4. **Теперь можно выполнять команды!**

---

### Способ 4: Через проводник Windows

1. **Откройте папку проекта** в проводнике
2. **В адресной строке** введите: `powershell` и нажмите Enter
3. **Терминал откроется** в этой папке автоматически!

---

## 🔍 Как понять, что вы в правильном месте?

### ✅ Правильно (Терминал):
```
PS C:\Users\Пётр Дулов\Desktop\Новая папка (9)> node --version
v18.17.0
```

### ❌ Неправильно (Консоль браузера):
```
> node --version
Uncaught SyntaxError: Unexpected identifier 'version'
```

---

## 📝 Пошаговая инструкция для новичков

### Шаг 1: Откройте VS Code
- Если не установлен: скачайте с [code.visualstudio.com](https://code.visualstudio.com/)

### Шаг 2: Откройте папку проекта
- File → Open Folder
- Выберите: `C:\Users\Пётр Дулов\Desktop\Новая папка (9)`

### Шаг 3: Откройте терминал
- Нажмите `Ctrl + ~`
- Внизу экрана появится терминал

### Шаг 4: Проверьте Node.js
В терминале введите:
```bash
node --version
```

**Должно показать версию**, например:
```
v18.17.0
```

Если показывает ошибку — Node.js не установлен. Скачайте с [nodejs.org](https://nodejs.org/)

### Шаг 5: Установите зависимости
```bash
npm install
```

### Шаг 6: Запустите сервер
```bash
npm run dev
```

---

## 🎯 Визуальное отличие

### Терминал (правильно):
```
┌─────────────────────────────────────┐
│ PS C:\...\Новая папка (9)>         │  ← Это терминал!
│                                     │
│ > node --version                    │
│ v18.17.0                            │
└─────────────────────────────────────┘
```

### Консоль браузера (неправильно):
```
┌─────────────────────────────────────┐
│ Console                             │  ← Это консоль браузера!
│ > node --version                    │
│ ❌ Uncaught SyntaxError...          │
└─────────────────────────────────────┘
```

---

## ❓ Частые вопросы

**Q: Я нажал F12 в браузере и вижу Console - это правильно?**  
A: НЕТ! Это консоль браузера для JavaScript. Нужен терминал системы.

**Q: Где находится терминал в VS Code?**  
A: Внизу экрана есть вкладка "Terminal". Или нажмите `Ctrl + ~`.

**Q: Как понять, что я в терминале?**  
A: Вы увидите путь к папке и символ `>` или `$` в конце строки.

**Q: Команды не работают, что делать?**  
A: Убедитесь, что:
- Вы в терминале, а не в консоли браузера
- Вы в правильной папке проекта
- Node.js установлен (`node --version` должен работать)

---

## 🚀 Быстрый старт (копируйте команды в терминал VS Code)

```bash
# 1. Проверка Node.js
node --version

# 2. Установка зависимостей
npm install

# 3. Создание .env.local (если еще не создан)
# Создайте файл через VS Code: New File → .env.local

# 4. Запуск сервера
npm run dev
```

**Все команды выполняйте в терминале VS Code!** 🎯

