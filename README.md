# Counter-Strike Auth System

Полноценная система регистрации и авторизации на Next.js 15 с Supabase.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Скопируйте `.env.local.example` в `.env.local`
3. Заполните переменные окружения:
   ```
   NEXT_PUBLIC_SUPABASE_URL=ваш_url_проекта
   NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
   ```

### 3. Настройка базы данных

Выполните SQL миграцию из файла `supabase/migrations/001_create_profiles_table.sql` в SQL Editor вашего Supabase проекта.

**Важно:** После выполнения миграции убедитесь, что таблица `profiles` создана и RLS политики применены.

### 4. Настройка Authentication в Supabase

В настройках Supabase (Authentication > Settings):
- **Включите анонимный вход:** Authentication > Settings > Enable anonymous sign-ins (включите переключатель)
- **Настройте Email/Password:** Убедитесь, что Email provider включен

### 5. Настройка OAuth провайдеров (опционально)

В настройках Supabase (Authentication > Providers):
- Включите Google OAuth и настройте Client ID и Secret
- Добавьте Redirect URL: `http://localhost:3000/auth/callback` (для разработки)
- Для продакшена добавьте URL вашего Vercel домена: `https://your-domain.vercel.app/auth/callback`
- Для VK (если поддерживается в вашем регионе) включите и настройте аналогично

### 6. Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📋 Функционал

- ✅ Регистрация по email + пароль
- ✅ Вход по email + пароль
- ✅ Вход через Google OAuth
- ✅ Анонимный вход (для быстрого доступа)
- ✅ Выход из аккаунта
- ✅ Автоматическое создание профиля в БД
- ✅ Защита маршрутов (редирект на главную если не авторизован)
- ✅ Автоматический редирект на /lobby после входа
- ✅ Toast уведомления
- ✅ Современный дизайн (glassmorphism)

## 🏗️ Структура проекта

```
├── app/
│   ├── actions/
│   │   └── auth.ts          # Server actions для авторизации
│   ├── auth/
│   │   └── callback/        # OAuth callback handler
│   ├── lobby/               # Защищенная страница
│   ├── layout.tsx           # Root layout
│   ├── page.tsx             # Главная страница с формами
│   └── globals.css          # Глобальные стили
├── components/
│   ├── AuthForms.tsx        # Компонент форм входа/регистрации
│   └── LogoutButton.tsx     # Кнопка выхода
├── lib/
│   └── supabase/
│       ├── client.ts        # Browser Supabase client
│       ├── server.ts        # Server Supabase client
│       └── middleware.ts    # Middleware для обновления сессии
├── middleware.ts            # Next.js middleware
└── supabase/
    └── migrations/          # SQL миграции
```

## 🔒 Защита маршрутов

Все маршруты кроме `/` защищены автоматически. Если пользователь не авторизован, он будет перенаправлен на главную страницу.

## 🎨 Стилизация

Проект использует Tailwind CSS с кастомной темой в стиле Counter-Strike:
- Темная цветовая схема
- Оранжевые акценты (#FF6B00)
- Glassmorphism эффекты
- Плавные анимации

## 📦 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения в настройках проекта:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. Деплой произойдет автоматически

## ⚠️ Важно

- **Анонимный вход:** Должна быть включена опция "Enable anonymous sign-ins" в настройках Authentication > Settings
- **OAuth провайдеры:** Убедитесь, что в Supabase включены нужные OAuth провайдеры (Google, VK)
- **Redirect URLs:** После деплоя обновите redirect URL в настройках OAuth провайдеров на ваш домен Vercel
- **Переменные окружения:** В Vercel добавьте `NEXT_PUBLIC_SITE_URL` со значением вашего домена (например, `https://your-app.vercel.app`)
- **База данных:** Убедитесь, что миграция выполнена и таблица `profiles` создана с правильными RLS политиками

