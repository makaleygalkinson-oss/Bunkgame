# CYBERPUNK 2077 - Система авторизации

Полнофункциональная система регистрации и авторизации через Supabase Auth для Next.js 15.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Создайте проект на [Supabase](https://app.supabase.com)
2. Скопируйте `SUPABASE_URL` и `SUPABASE_ANON_KEY` из настроек проекта (Settings > API)
3. Создайте файл `.env.local` и добавьте:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 3. Настройка базы данных

В Supabase Dashboard перейдите в SQL Editor и выполните SQL из файла `supabase/migrations/001_create_profiles_table.sql`:

```sql
CREATE TABLE IF NOT EXISTS public.profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own profile"
  ON public.profiles FOR SELECT
  USING (auth.uid() = id);

CREATE POLICY "Users can update own profile"
  ON public.profiles FOR UPDATE
  USING (auth.uid() = id);

CREATE POLICY "Users can insert own profile"
  ON public.profiles FOR INSERT
  WITH CHECK (auth.uid() = id);
```

### 4. Настройка OAuth провайдеров (опционально)

#### Google OAuth:
1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите OAuth 2.0 и создайте Client ID (тип: Web application)
3. Добавьте Redirect URL: `https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback`
   - Замените `YOUR_PROJECT_REF` на ваш Project Reference из Supabase
4. Скопируйте Client ID и Client Secret
5. В Supabase Dashboard: Authentication > Providers > Google
6. Включите Google провайдер и вставьте Client ID и Client Secret

> 📖 **Подробная инструкция:** См. файл `GOOGLE_OAUTH_SETUP.md`

#### VK OAuth (если доступен):
1. В Supabase Dashboard: Authentication > Providers > VK
2. Включите VK провайдер
3. Настройте согласно документации Supabase

### 5. Запуск проекта

#### 📋 Пошаговая инструкция:

**Шаг 1: Проверьте наличие Node.js**

Убедитесь, что у вас установлен Node.js (версия 18 или выше):

```bash
node --version
npm --version
```

Если Node.js не установлен, скачайте его с [nodejs.org](https://nodejs.org/)

**Шаг 2: Откройте терминал в папке проекта**

- **Windows**: Откройте PowerShell или Command Prompt в папке проекта
- **Mac/Linux**: Откройте Terminal в папке проекта

**Шаг 3: Установите зависимости (если еще не установлены)**

```bash
npm install
```

Эта команда установит все необходимые пакеты из `package.json`.

**Шаг 4: Убедитесь, что файл `.env.local` настроен**

Проверьте, что файл `.env.local` существует и содержит ваши ключи Supabase:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**Шаг 5: Запустите проект**

```bash
npm run dev
```

После выполнения команды вы увидите сообщение:

```
  ▲ Next.js 15.x.x
  - Local:        http://localhost:3000
  - Ready in Xs
```

**Шаг 6: Откройте браузер**

Перейдите по адресу: [http://localhost:3000](http://localhost:3000)

#### ⚠️ Возможные проблемы:

**Ошибка: "npm run dev" не работает**
- Убедитесь, что вы находитесь в правильной папке проекта
- Проверьте, что `package.json` существует и содержит скрипт `dev`
- Попробуйте удалить `node_modules` и `package-lock.json`, затем выполните `npm install` заново

**Ошибка: "Port 3000 is already in use"**
- Закройте другое приложение, использующее порт 3000
- Или запустите на другом порту: `npm run dev -- -p 3001`

**Ошибка: "Cannot find module"**
- Выполните `npm install` для установки всех зависимостей

**Проект не запускается**
- Проверьте, что файл `.env.local` настроен правильно
- Убедитесь, что все зависимости установлены: `npm install`

## ✨ Функционал

- ✅ Регистрация по email + пароль
- ✅ Вход по email + пароль
- ✅ Вход через Google OAuth
- ✅ Анонимный вход (для быстрого доступа к игре)
- ✅ Автоматическое создание профиля в БД
- ✅ Защита маршрутов (middleware)
- ✅ Автоматический редирект на /lobby после входа
- ✅ Toast уведомления (sonner)
- ✅ Современный UI в стиле Cyberpunk

## 📁 Структура проекта

```
├── app/
│   ├── auth/
│   │   └── callback/        # OAuth callback handler
│   ├── lobby/               # Защищённая страница
│   ├── globals.css          # Глобальные стили
│   ├── layout.tsx           # Root layout
│   └── page.tsx             # Главная страница
├── components/
│   ├── Header.tsx           # Шапка с кнопками авторизации
│   ├── Hero.tsx             # Hero секция
│   └── NewsSection.tsx      # Секция новостей
├── lib/
│   ├── actions/
│   │   └── profile.ts       # Server action для создания профилей
│   └── supabase/
│       ├── client.ts        # Supabase клиент для клиента
│       ├── server.ts        # Supabase клиент для сервера
│       └── middleware.ts    # Middleware для обновления сессии
├── hooks/
│   └── useAuth.tsx          # Хук для работы с авторизацией
├── middleware.ts            # Next.js middleware
└── supabase/
    └── migrations/          # SQL миграции
```

## 🔒 Защита маршрутов

Все маршруты кроме `/`, `/login`, `/register` защищены middleware. Неавторизованные пользователи автоматически перенаправляются на главную страницу.

## 🎨 Стилизация

Проект использует Tailwind CSS с кастомными цветами в стиле Cyberpunk 2077. Все стили определены в `tailwind.config.ts` и `app/globals.css`.

## 📝 Примечания

- VK OAuth может быть недоступен в некоторых регионах (закомментирован в коде)
- Анонимный вход создаёт временную сессию без email
- Профили создаются автоматически при любой форме входа
- Все данные профилей защищены Row Level Security (RLS)

## 🛠 Технологии

- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS
- Supabase Auth
- Sonner (Toast уведомления)

