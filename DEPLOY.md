# 🚀 Инструкция по деплою

## Подготовка

### 1. Настройка Supabase

1. Перейдите на [supabase.com](https://supabase.com) и создайте новый проект
2. Дождитесь завершения создания проекта
3. Перейдите в **SQL Editor** в панели управления Supabase
4. Откройте файл `supabase/schema.sql` из этого проекта
5. Скопируйте весь SQL код и выполните его в SQL Editor
6. Перейдите в **Settings** → **API**
7. Скопируйте:
   - **Project URL** (это будет `NEXT_PUBLIC_SUPABASE_URL`)
   - **anon public** key (это будет `NEXT_PUBLIC_SUPABASE_ANON_KEY`)

### 2. Настройка GitHub

1. Создайте новый репозиторий на GitHub
2. Инициализируйте git в проекте:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### 3. Настройка Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub аккаунт
3. Нажмите **Add New Project**
4. Выберите ваш репозиторий
5. В настройках проекта добавьте переменные окружения:
   - `NEXT_PUBLIC_SUPABASE_URL` - ваш Project URL из Supabase
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - ваш anon key из Supabase
6. Нажмите **Deploy**

### 4. Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env.local` в корне проекта:
```
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_supabase_anon_key
```

3. Запустите dev сервер:
```bash
npm run dev
```

4. Откройте [http://localhost:3000](http://localhost:3000)

## Структура проекта

```
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница
│   ├── create/            # Создание комнаты
│   ├── join/              # Присоединение к комнате
│   ├── rules/             # Правила игры
│   ├── lobby/[roomId]/    # Лобби комнаты
│   └── game/[roomId]/     # Игровая комната
├── components/            # React компоненты
├── lib/                   # Утилиты и логика
│   ├── supabase.ts       # Supabase клиент
│   ├── store.ts          # Zustand store
│   └── gameLogic.ts      # Игровая логика
├── supabase/             # SQL схемы
└── public/               # Статические файлы
```

## Важные замечания

- **RLS (Row Level Security)**: В схеме БД включены политики для публичного доступа. Для продакшена рекомендуется настроить более строгие политики.
- **Real-time**: Используется Supabase Realtime для синхронизации между игроками
- **Хостинг**: Vercel автоматически деплоит при каждом push в main ветку

## Решение проблем

### Ошибка подключения к Supabase
- Проверьте правильность URL и ключа в переменных окружения
- Убедитесь, что RLS политики настроены правильно

### Real-time не работает
- Проверьте, что Realtime включен в настройках Supabase проекта
- Убедитесь, что таблицы имеют правильные политики RLS

### Ошибки при сборке
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте версии Node.js (рекомендуется 18+)

